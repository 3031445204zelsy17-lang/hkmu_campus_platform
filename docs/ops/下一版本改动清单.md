# 下一版本(v1.11)改动清单

> 攒一批前端改动 + 配套后端,**一次提审**。提审期间正式版 v1.10 照常运行;审过**手动发布**。
> 体验版可先免审给 admin 用(功能验证/巡逻)。
>
> 维护说明:想到新的优化项就往里加(勾选状态)。提审前确认本清单都处理完。

---

## 🔵 前端(上正式版必须提审,1–3 天)

- [x] **帖子删帖入口(admin)** — admin 进别人帖子可见 🗑 删除。
  - 已改:`miniprogram/utils/auth.js`(`decorateUser` 加 `isAdmin: user.identity === "admin"`)+ `miniprogram/pages/post-detail/post-detail.wxml`(删帖按钮条件加 `|| user.isAdmin`)
  - 后端 `posts.py` 本就支持 admin 删,无需改后端
- [ ] **帖子分享(post-detail)** — 加 `onShareAppMessage`(path `/pages/post-detail/post-detail?id=<postId>` 回到该帖,不带邀请码)。profile 已有(邀请分享,不动)。复制链接跟着转发的启用一起亮。
- [ ] **失物删帖入口(admin)** — admin 能删失物帖。
  - 待改:`lostfound.wxml`(删除按钮独立,admin 可见;编辑/标记仍只作者)+ 后端 `lostfound.py:189` delete 加 admin 分支
- [ ] **profile 加「用户协议」入口** — 仿 `profile.wxml:56` 隐私政策那行,加一行「用户协议」→ `/pages/terms/terms`(登录后可随时查看,含第 9 段禁止行为)。现状:协议只在登录页有入口,登录后看不到。
- [ ] **compose 发帖页加违规警告(直接显示)** — 发帖页顶部提示条,文案:「⚠️ 发布内容请遵守社区规范:禁止违法、色情、暴力、广告、不实信息等;违规将被删除,严重者封禁账号。」(发帖前即时看到,不用点链接)
- [ ] **首次进社区弹协议摘要 + 「我知道了」** — 首次进社区页弹窗,展示规范摘要(引用第 9 段禁止行为),点「我知道了」关闭并记 storage(不重复弹)
- [ ] **🐛 修帖子分类标签不跟语言切换** — 根因:发帖提交翻译文字(`compose.js:130` 用 `this.data.category` 文字而非 `categoryKey`),DB 存文字,显示直接用(`post.js:39 item.category` 不翻译)→ 标签永远卡在发帖者当时的语言。修:① `compose.js` 提交 `categoryKey`;② `post.js:39` 改 `text.categories[item.category] || item.category`(key 翻译 + 旧文字 fallback);③ `i18n.js` home/community bundle 补 `categories`(campus/course/life/activity/help 三语言);④ 后端 `posts.py` category 校验改 key;⑤ 旧帖数据靠 `|| item.category` 兼容显示(不迁移)
- [ ] **课程名繁→简 + locale 选列(跨端)** — 现状:`course_catalogue.display_name` 混合(545 繁中人文课 CHIN/PTH/CCA/TRAN + 4126 英文)。改:① 加 `name_zh_cn` 列;② OpenCC 把 545 繁中转简体填入(英文课留空 fallback);③ 后端 catalogue 端点返回 `name_zh_cn`;④ 前端课程名按 locale 选(`zh-CN → name_zh_cn || display_name`,其他 → `display_name`)。**后端(加列+数据+返回)免审先部署,前端选列搭 v1.11 提审**。~1–1.5 天,繁简转换质量高(非机翻)

---

## 🟠 后端(免审,部署即生效)

- [ ] **失物 `delete_item` 加 admin 分支** — `backend/app/routers/lostfound.py:189`(配合失物删帖入口)
- [ ] _(方向②:第三方审核替换微信 msg_sec_check,绕开 IP 白名单 + 补 FR1c —— 长期,单独计划,见 task #4)_

---

## ⚪ 待 commit(生产已部署/待提审,但代码还没入库)

- [ ] 步骤1 后端:`backend/app/config.py` + `backend/app/services/content_security.py` + `backend/tests/test_content_security_fr1.py`(生产已跑 `modkill-step1`)
- [ ] 步骤2 前端:`miniprogram/utils/auth.js` + `miniprogram/pages/post-detail/post-detail.wxml`
- [ ] docs:`docs/ops/正式上线checklist.md` + `docs/ops/内容审核503-故障排查与解决方案.docx`

---

## 📌 背景状态(供参考)

- **审核降级中**:`ENABLE_CONTENT_MODERATION=false`(v1.10 发帖 503 的临时降级,方向② 落地后改回 `true`)
- **admin 已配**:id=8(司徒 Ryan Szeto)+ id=10(SYF Zelsy17);`ADMIN_USER_IDS=10,8`(Azure appsettings,DB identity 已 admin)
- **IP 白名单路已放弃**:Azure 32 个动态出站 IP ✗ 微信白名单 10 条上限 + 不收 /22 CIDR,架构性堵死(别再试)
- **部署坑**:Azure Web App restart 用缓存同名 `latest`,**必须用唯一 tag + `az webapp config container set` 强制拉新**(见 memory `launch-domain-icp-blocker`)
- **提审流程**:开发者工具上传 → 设体验版(免审,admin 先验)→ 提审 → 通过 → 手动发布(正式版替换 v1.10,时机你定)
