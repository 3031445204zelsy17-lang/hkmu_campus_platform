# ⛔ 本目录已禁用(2026-08-19,选课数据修复批次 1)

本目录 14 份 JSON 全部产自**丢行解析器**(pypdf 静默丢 ~636 码行,20.6%),
其结论已被独立复核推翻,详见 `../复核报告-2026-08-19.md`:

| 旧产物 | 错在哪 |
|---|---|
| `official_ge_codes.json` | GE 目录 92(实为 89,多出 3 个互斥引用幻影码) |
| `year_mismatch.json` | 年份错位 130/46(实为 155/48,含 3 假阳、漏 28 真错) |
| 其余抓取/比对中间产物 | 同源丢行解析,数字一律不可引用 |

**纪律:任何断言、施工清单、灌库脚本不得引用本目录文件。**
可信数据在 `../verify/`(双引擎 × 多锚点收敛,入库供 CI 断言用:
`test_course_data_coverage.py`);年份施工清单以复核报告第四节 155 条为准
(批次 2),不得用 `year_mismatch.json`。

已核对(批次 0 验收):backend/scripts/.github 全文零引用本目录;
并有测试 `test_results_dir_not_referenced` 机器锁死,新增引用会红。
