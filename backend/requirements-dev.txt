# Dev/test-only dependencies — NOT installed in the production image.
# The Dockerfile runs `pip install -r backend/requirements.txt` only, so adding
# test tooling here keeps the prod image lean. CI (and local devs) install both.
pytest>=8.0
pytest-asyncio>=0.23
# httpx is already in backend/requirements.txt and is reused here as the
# in-process ASGI test client.
